import s from "./History.module.css";
const History = () => {
  return (
    <div className={s.history}>
      <h2 className={s.history_title}>History words</h2>
    </div>
  );
};

export default History;
